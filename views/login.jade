extends layout

block content
  h1= title
  p Welcome to #{title+(user?user.username:"")}
  form(path="/login" method="POST")
    input(type="text" name="username")
    -if(err){
      #{err}
    -}
    br
    input(type="password" name="psw")
    button(type="submit" value="submit") submit
